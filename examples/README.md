# Public Examples

These fixtures are synthetic. They use relative paths, placeholder actors, and
example.test URLs so they can be shared without exposing local runtime data.

- `agent_run.jsonl`: signed JSONL receipt record
- `receipts/run_example_0001.md`: Markdown rendering of the same record
- `source/demo-run.jsonl`: tiny dummy source input used for the example
